fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-multipart>=0.0.9
requests>=2.31

# Optional: enables real audio tempo/energy analysis in app/audio_analysis.py.
# The rest of the app works without them (lyrics-only modes, mock generator).
librosa>=0.10
numpy>=1.26

# Dev/test
pytest>=8.0
