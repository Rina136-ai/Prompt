# Copy to .env and fill in to enable real generation via Higgsfield.
# Without this, the app runs fully offline using the mock generator.
HIGGSFIELD_API_KEY=
HIGGSFIELD_BASE_URL=https://api.higgsfield.ai
